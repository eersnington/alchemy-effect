import { describe, test } from "vitest";

describe("EventBridge Bindings", () => {
  describe("DescribeEventBus", () => {
    test.skip("describes a bound event bus", () => {});
  });

  describe("ListEventBuses", () => {
    test.skip("lists event buses in the account", () => {});
  });

  describe("DescribeRule", () => {
    test.skip("describes a bound rule", () => {});
  });

  describe("ListRules", () => {
    test.skip("lists rules on the selected event bus", () => {});
  });

  describe("ListTargetsByRule", () => {
    test.skip("lists the targets for a bound rule", () => {});
  });

  describe("PutEvents", () => {
    test.skip("publishes events to the bound bus", () => {});
  });

  describe("TestEventPattern", () => {
    test.skip("matches an event against a pattern", () => {});
  });
});
